const conditionCase = (info, condition, value) => {
  switch(condition) {
    case 'Maior que':
      const popFilter = console.log('oie');
      return popFilter;
    case 'Menor que':
      const popFilter = console.log('oie');
      return popFilter;
    case 'Igual a':
      const popFilter = console.log('oie');
      return popFilter;
    default:
      return [];
  }
}

export default conditionCase;
